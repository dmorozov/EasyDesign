**Checkbox** — square multi-select control with an animated check-in.

```jsx
<Checkbox label="Clip content" defaultChecked />
<Checkbox label="Show grid" />
<Checkbox label="Locked" disabled />
```

Wraps a native checkbox input — pass `checked`/`defaultChecked`, `onChange`, `disabled`.
